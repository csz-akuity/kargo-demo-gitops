apiVersion: v1
kind: Service
metadata:
  name: kargo-demo
  labels:
    team: beta
    environment: development
spec:
  selector:
    app: kargo-demo
    team: beta
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 80
  type: LoadBalancer
